pour exécuter l'application ;
1- installer angular npm packages(node modules).
vous devez ouvrir le dossier angular-frontend dans l'invite de commande
 et tapez "npm install" 
2-intaller python-multipar module
vous devez ouvrir le dossier backend dans l'invite de commande
 et tapez "pip install -r requirements.txt" 